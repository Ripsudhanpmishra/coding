#include<stdio.h>
int main(){
    int arr[6]={1,2,3,4,5,6,8},pos;
    // printf("Enter the position to edit: ");
    // scanf("%d",&pos);
    for (int i = 0; i <= 9; i++)
    {
        printf("%d ",arr[i]);
    }
    
    
    return 0;
}